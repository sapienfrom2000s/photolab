<p style="color: green"><%= notice %></p>

<h1>Photo Products</h1>
<%= link_to "New Photo Product", new_photo_path, :class => ['button', 'is-info'] %><br>
<div id="photos" class='mt-4'>
  <% @photos.each do |photo| %>
    <p> 
    <%= link_to "#{photo.title}", photo %>
    </p>
  <% end %>
</div>

